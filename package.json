{
  "name": "vedaai-assessment-creator",
  "version": "1.0.0",
  "private": true,
  "description": "VedaAI — AI-powered assessment creator monorepo",
  "packageManager": "npm@10.9.2",
  "workspaces": [
    "apps/api",
    "apps/web",
    "packages/shared"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "build:api": "npm run build -w @vedaai/shared && npm run build -w @vedaai/api",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "mongodb-memory-server": "^11.1.0",
    "turbo": "^2.3.3",
    "typescript": "5.5.3"
  }
}
